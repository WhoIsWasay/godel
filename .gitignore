# Environment
.env
.env.local
.env.*

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.pycache
*.egg-info/
dist/
build/
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.suo
*.user

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Z3 temp files
*.py.tmp

# Model weights / embeddings (too large for git)
*.bin
*.pt
*.ckpt

# Foundry build artifacts / dependencies (restored via forge install / .gitmodules)
lib/
cache/
out/
broadcast/

# Pipeline-generated output (findings, reports, proofs, fixes, submissions)
output/
output-grandtest/
lendingPool/
*.xml

# Gatekeeper materializes audited contracts here at runtime (scratch)
src/

# Data pipeline scripts / corpus (internal tooling, too large for git)
dataretlated/
dataretlated/*.json
!dataretlated/sample_findings.json

# DB dumps
*.sql
*.dump

# Scratch and test files
test.py
zeethree.py
tmp_prop_check.py
index.html

# Docker / infra config with local secrets
docker-compose.yml

# Contract scratch folders
solContracts/
solCon/

# Private benchmark corpus + sample fixtures (personal data, stays local)
TestsDone/
testOfCode/fixtures/
# External audit-contest benchmark targets (large flattened contracts, local only)
Fv-audits/
# Live-audit proof bundle — kept local, not part of the submission repo
hackathon_proof/
