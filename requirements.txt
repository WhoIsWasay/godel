# Gödel pipeline dependencies
langchain-core==1.4.8
langchain-openai==1.3.3
langgraph==1.2.6
openai==2.43.0
python-dotenv==1.2.2
z3-solver==4.16.0.0

# Static analyzer / CFG extraction (Phase 1 abstracter layer)
slither-analyzer>=0.11.0
solc-select>=1.2.0

# RAG bridge (Infrastructure/postgres.py + domain/rag.py) — previously
# missing, which silently disabled RAG in Docker/CI (C4 fix)
psycopg2-binary>=2.9.9
httpx>=0.27.0
sentence-transformers>=3.0.0

# MCP server (mcp_server.py)
fastmcp>=2.0.0
