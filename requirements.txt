# Gödel pipeline dependencies
langchain-core==1.4.8
langchain-openai==1.3.3
langgraph==1.2.6
openai==2.43.0
python-dotenv==1.2.2
z3-solver==4.16.0.0

# MCP server only (Phase 7 face) — not needed for CI/CD or the pipeline:
#   pip install fastmcp

